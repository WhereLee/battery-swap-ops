# 批次1 复审与优化记录（S1 代码两轮 review）

> 性质：S1 交付后的自查复审（用户指示：启动 MQ + 多次审查反思优化）。
> 状态：两轮完成；单测 33/33；门禁剧本复跑通过（2026-09-11）。
> 关联：`批次1-骨架与指令闭环.md`（S1 交付记录）

---

## 零、环境补装：RocketMQ 启动

| 项 | 结果 |
|---|---|
| 启动方式 | `D:\rocketmq-5.3.1\start-all-detached.bat`（新增：WMI 脱离启动，避免工具杀进程树） |
| 进程 | namesrv 9876 / broker 10911 / proxy-gRPC 8081 全部监听 |
| broker 注册 | `broker-a @ 127.0.0.1:10911`（broker.conf 生效，与范例 2026-09-10 修复一致） |
| topic | `swap-device-event` 已建（read/write 队列数=1，S3 保序前提） |
| 备注 | AGENTS.md 声称 MQ 常驻，实际未在跑；本次起服务后以端口/集群列表实测为准 |

## 一、第一轮 review：正确性 / 健壮性（7 项，全部修复）

| # | 发现 | 处置 |
|---|---|---|
| R1-1 | `CommandLogService` 缺启动 seq 对齐（S0.5 已设计未实现）：Redis 丢失/回退后 INCR 从 1 重来，撞 `uk_cabinet_seq` | 移植范例 LUA 对齐：`max(Redis, DB MAX)`，失败不阻断启动 |
| R1-2 | 事件语义缺一致性守卫：BATTERY_OUT/IN 不校验"事件电池 vs 仓内登记"；SOC_REPORT 会把 LOANED 电池改回在仓态 | 加台账不一致 warn（事件仍为准，留痕给 S3 对账）；非在仓电池只更新 soc 不改状态 |
| R1-3 | 下行链路号取两次（流水 traceId 与请求头可能不同号，grep 断链） | 只取一次并复用 |
| R1-4 | 查询签名未定义（S0.3 §2.4 悬空；sim 查询实际用空 canonical） | 契约新增 `canonicalQuery = cabinetNo\|QUERY\|0` + 固定向量；sim 查询验签改用之 |
| R1-5 | sim 密钥只查空，未校格式（与 seeder 的 32hex 不一致） | fail-fast 校 `^[0-9a-f]{32}$` |
| R1-6 | sim 联调端点 IAE 裸 500 | 新增 `SimExceptionHandler`（400/透传/500） |
| R1-7 | `HttpEventReporter` 死字段 ObjectMapper；溢出腾位竞态会静默丢新事件 | 清理；溢出路径显式告警（丢旧/丢新分别留痕） |

第二轮（对照 S0 文档逐条核）：补齐的心智/质量项
- `DeviceEventService` 一次加载柜档案（去掉 requireCell 内重复查询）；
- `CabinetSim.snapshot()` 加锁拷贝（并发迭代安全）；
- 心跳自述状态按空仓计算（无空仓 → FULL(2)，S0.3 §2.1 语义）；
- 新增测试：seq 对齐（成功/失败容忍）、SOC 守卫（借出不改态/在仓达阈转 FULL）、心跳状态回落。

## 二、验证

- 单测 **33/33 绿**（契约 4 + 平台 23 + 模拟器 6）；
- 门禁剧本复跑（重构后）：`commandSeq=4 → DOOR_OPENED → ARRIVED`，10/10 柜在线，**GATE-1 PASS**；
- CI：见同批推送 run（三模块 `mvn verify`）。

## 三、遗留与后置（明确不做/后做）

| 项 | 归属 |
|---|---|
| 管理端 CRUD/RBAC | S2（业务 CRUD）/ S4（RBAC 可选，取舍已在 S0.4 标注） |
| QUERY_STATE 平台侧调用与对账 | S3 |
| MQ 事件通道（producer/consumer） | S3（topic 已就绪） |
| sim 上报丢弃 → 队首持留 | S3 |
| 生产配置（DB 凭据 ENC/环境变量、profile 收敛） | S5 部署前 |
| `swap_order`/`payment_record` 表已建但代码待 S2 | S2 |
