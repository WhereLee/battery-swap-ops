# 批次14 —— S7 运营纵深收口（2026-09-16）

## S7 全景（WP-0 → A → C → D → B + 韧性补丁）

| 批次 | 内容 | 关键提交 | 证据 |
|---|---|---|---|
| WP-0 | 押金二次退款修复（+退款状态白名单） | `5652ec2` | fixes/deposit-double-refund.md |
| WP-A | 管理端 RBAC（4 角色×38 权限码）+ 操作审计 + break-glass | `1d74cdd`/`3d89173`（云） | `_c17` 18/18 |
| WP-C | 渠道对账 T+1（四类差异/处置留痕/日任务） | `ce70dcf`/`d9b2729`（云） | `_c19` 10/10 |
| WP-D | 报障→工单 / 欠费闭环 / 优惠券 / 站内信 | `bbd06bd`/`7642a44`（云） | `_c20` 18/18 |
| 韧性补丁 | G1 计费欠费化 / G2 报障复核 / G3 对账互斥 | `8c2bb8a` | `_c20` 16-18 |
| WP-B | 代理分润结算（冲正/补行/结算单） | `6c04c11` | `_c18` 12/12 |

## 全量回归（2026-09-16，最终代码）

- 基座 `_g1`（S1 开仓闭环）exit=0；
- S7 五条剧本全 PASS：`_c16` 16/16、`_c17` 18/18、`_c18` 12/12、`_c19` 10/10、`_c20` 18/18；
- 单测 375/375（clean verify + 随机序）；对账不变量 14 组；
- 说明：运行编排曾遇"sim 继承管道句柄导致父进程等待"的脚本现象（非产品问题），
  改用逐条 detached 运行完成（编排脚本已弃用删除）。

## 交付面总结（S7 新增）

- **身份与审计**：会话登录 + 角色矩阵 + 方法级鉴权 + 操作审计（覆盖性守卫测试）+ break-glass；
- **经营闭环**：渠道对账（差异处置）→ 用户服务（报障/欠费/券/站内信）→ 代理分润（流水分账/冲正/结算单）；
- **韧性**：计费硬失败不再回滚已交付事件（欠费化+门槛拦截）；报障不重复开单；对账 IO 互斥；
- **可观测**：对账 14 组（新增 5 组）+ 看护 9 任务 + 四类站内信触达。

## 声明与后置（如实）

- 数据权限（DataFilter）、月卡分账基数、券折扣型、打款真实通道、站内信推送通道、周期自动生成结算单；
- G4（超期转人工 vs 归还同秒）/G5（券发放并发）接受声明（fixes/resilience-patch.md）。

## 云端

- 迁移 db/11-15 已应用（每次迁移前 backup.sh 快照）；终态 jar 部署与结算冒烟见 rollout-wpb 记录（服务器连接文档 §九）。
