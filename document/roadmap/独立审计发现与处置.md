# 独立审计发现与处置台账

> 来源：2026-09-19 一次**独立只读代码审计**（subagent，无本项目上下文，覆盖 `swap-server` 的
> order / settlement / user / common / device / admin / workorder / transfer / reconcile / payrecon /
> outbox / asset / alarm / charge / agent / dashboard / web / config / dev 各域，含反证自查与覆盖范围声明）。
> 目的：把审计结论**逐条落账**，避免"修了 P0 就忘了 P1/P2"，也避免"审计说什么就信什么"。
> 纪律：每条都标注**我的复核结论**（确认 / 待判定 / 不成立），修复必须独立成批并带回归网。
> 关联：P0 的完整根因与修复见 `document/pitfalls/tx-self-invocation-refund-double-credit.md` 与
> `document/block-records/批次32-退款事务边界与自调用守卫.md`。

---

## 一、台账

| 编号 | 严重度 | 结论 | 复核结论 | 处置 | 状态 |
|---|---|---|---|---|---|
| AUD-1 | P0 | `RefundService.apply` 的 `@Transactional` 因**同类自调用**失效 → 加款已提交而 CAS 未做，重驱动时二次入账（资金双记） | **确认**：复核了类上无注解、无 AspectJ 模式配置、两条入口（在线退款 vs 延迟重投）事务语义不对称、调用方无外层事务 | 批次32 修复：改编程式事务；WAIT 重驱动；三层回归网 | ✅ 已修 |
| AUD-2 | P1 | 退款单 `WAIT` 无任何重驱动路径（`doRefund` 命中既有单直接返回；`DelayQueueService.deadSize()` 无调用方） | **确认**：与 AUD-1 同源，同批修复（命中 WAIT 时重驱动） | 批次32 | ✅ 已修 |
| AUD-3 | P1 | 管理端登录 IP 限流取 `X-Forwarded-For` **第一个**元素 → 一行 header 即可绕过撞库防护 | **确认**：`RateLimitAspect.clientIp()` 取 `split(",")[0]`；云上加了 nginx 反代后 `$proxy_add_x_forwarded_for` 会把真实对端**追加在最右**，左起第一段仍是攻击者可控 | **批次33 已修**：新增 `ClientIpResolver`（默认不信任 XFF、受信代理才解析且取最右非受信、支持 CIDR）；`trusted-proxies` 默认空；部署脚本幂等配置 `127.0.0.1` 并自检。证据 `_c36` 5/5 | ✅ 已修 |
| AUD-4 | P2 | 月卡 `dailyLimitTimes` 全链路未落地（只有建表/表单/校验，无按日计次；`BillingService` 注释称"日限在创建时已校验"不成立） | **确认**：回源 S0.1 领域模型第 53 行确有该字段且未标"v1 仅记录"（同类会标注的写法见 `settlementCycle`）⇒ 属"设计声明了但实现漏了"，不是有意留后；原注释是误导 | **批次34 已补**：计费月卡分支接日限判定（自然日口径、`SELECT ... FOR UPDATE` 行锁把"数单量+放行"做成临界区、只数 `payType=PLAN` 的单、超限回落余额计费）；`BillingServiceTest` +4 例。无实机剧本（需跨自然日数据），已申报 | ✅ 已补 |
| AUD-5 | P2 | 分账冲正按"代理**当前** `shareBp`"重算，未快照原单比例；比例被改后冲正金额与原单口径不一致，且对账只校验单行守恒，发现不了跨行漂移 | **确认**：`SettlementService.writeLine(agentFromOrigin=true)` 只快照 `agentId`，比例现读；`AgentService.update` 允许随时改比例 | **批次35 已修**：新增 `writeOriginProportionalLine`——冲正/补缴一律按原单比例派生（`amount × origin.agentShareFen ÷ origin.baseAmountFen`，同号取整），原单基数为 0 时全记平台并告警；`writeLine` 收窄为只写 ORDER 行。`SettlementServiceTest` +2（含"比例下调后冲正不漂移"） | ✅ 已修 |
| AUD-6 | P2 | 同订单第二笔**部分**退款不落 `payment_record`：`uk_order_type(order_id, payment_type)` 与部分退款语义冲突，`DuplicateKeyException` 被当作"幂等命中"吞掉，且 9 处调用方都不看返回值 | **确认**：`refund_record` 唯一键是 `(order_id, reason)`，所以"不同原因的两笔退款"合法，但第二笔流水写不进去；钱包金额正确、**台账漏记** | **批次35 已修**：db/18 用生成列把幂等闸收窄（REFUND 与无订单行为 NULL，其余仍 `orderId:type`），`uk_trade_no` 保留；实机 `_c37` 13/13（含"扣费幂等闸未削弱"与"两笔真实退款两行流水"） | ✅ 已修 |
| AUD-7 | P2 | dev 支付 mock：`page()` 把 `tradeNo` 原样拼进 HTML（反射型 XSS）；`notify()` 无鉴权且用服务端密钥自签即入账（造币入口） | **确认**：仅 `swap.dev.enabled=true` 时可达（默认 false），故为 P2 而非 P0 | **批次36 已修**：支付页单号走白名单 `^[A-Za-z0-9-]{4,64}$`（并写明放宽时必须补转义）；notify 限定回环 + 地址走受信代理感知的 `ClientIpResolver`（反代后的远程请求不得伪装成本机）+ `result` 白名单 + 每次入账打 WARN；`PayMockControllerTest` 6 例 + 实机探针 4 项 | ✅ 已修 |
| AUD-8 | P2 | 管理台标签文字**对比度不达 WCAG AA**：Element Plus 默认浅色 tag 把基色当文字压在浅底上（success 2.08:1 / warning 2.04:1 / danger 2.61:1 / info 2.80:1），dark 效果白字压基色 3.08:1；12px 属正常字号，要求 4.5:1 | **确认（实测量化）**：`_c38` 逐标签读渲染后的前景/背景并算比值，134 个标签低于 3:1；复核用独立脚本按 WCAG 公式复算，与探针一致 | **批次38 已修**：`global.css` 按色相压深文字（浅色 tag）/底色（dark tag），一套 5 色板；门禁阈值同时从 3:1 **提高**到 4.5:1（不放宽标准以迁就实现）。实测 270 个标签 6.00–8.57:1 | ✅ 已修 |
| AUD-9 | P2 | 换电柜详情 **SOC 数字从不显示**：`:status="soc>=90?'success':undefined"` 会让 `el-progress` 用**状态图标替换百分比文字**，占用仓位只剩绿条+对勾 | **确认（实测量化）**：`_c38` 测得该列 12 行"可见为空、DOM 文本 0、子元素 10"，而同页接口对 6 个占用仓位返回 `soc=100`；读 Element Plus progress 源码确认 `STATUS_COLOR_MAP` + `CircleCheck/CircleClose/WarningFilled` 分支 | **批次38 已修**：改 `:color` 传色（`socColor`），保留数字；视觉复核确认"进度条 + 100% 文字，无对勾图标" | ✅ 已修 |
| AUD-10 | P2 | **门禁自身缺陷**（不是应用缺陷）：`_c35_out.txt` 声称 31/31 而脚本只有 25 个检查；同批重构又把标签字符串传进 `waitFor` 的超时参数（NaN ⇒ 立即失败 ⇒ 9 项 0ms 全红）；证据文件是人工 tee 的一次性产物，无命令可复现 | **确认（可复现）**：`Select-String '^\s*check\('` 数出 25 与文件里的 31 不符；`Date.now() + "work-order detail"` = NaN 直接推得 | **批次38 已修**：`waitFor` 对非法超时**抛错**；恢复 6 条导航检查并改显式签名（25+6=31）；`_c34`/`_c35`/`_c38` 三个剧本改为**运行时自写** `_*_out.txt` | ✅ 已修 |

## 二、审计明确判定"不成立"的项（避免重复怀疑）

审计对以下项做了反证并判为不成立，我认可其推理，登记在此以便后续不再重复排查：

| 项 | 为什么不成立 |
|---|---|
| `AllocationService` 并发超卖 | Redis `SPOP` + Lua `SETNX` 与 DB `lock_order_id IS NULL` 条件更新双层；`unlockAndDeleteLock` 无条件删 Redis 锁确实存在，但 **DB 条件更新是最终判官**，构造不出真实双分配 |
| `BillingService` 资金动作先于 `payment_record` | `charge` 只在订单 CAS 成功分支内调用，且整条事件链在 `DeviceEventService.handle` 同一事务内，重放被 CAS 挡住（与 AUD-1 的区别正是 AUD-1 缺事务） |
| MyBatis-Plus 分页拦截器缺失 | 已在批次30 修复（`config/MybatisPlusConfig`），并有 `_c33` 与 `MybatisPlusConfigTest` 两层回归网 |
| `@PreAuthorize` 覆盖 | 反射守卫 `AdminEndpointGuardTest` + 全量 grep 核对，含批次31 新增的 4 个视图端点 |
| 雪花/WorkerId、延迟队列 claim、outbox CAS、MQ 分级 ack、TaskWatchdog、两级缓存、数据权限 fail-closed | 读过，未发现 P0/P1 |

## 三、审计未覆盖（结论边界，需后续补）

1. `swap-sim` / `swap-agent` / `swap-web` 三模块**未读**（审计聚焦 `swap-server`）。
2. `entity/form/dao/enums` 绝大多数未逐字读 ⇒ **入参边界类**结论不完整。
3. `db/04`~`db/11`、`db/13`、`db/16`、`db/17` 的约束细节未核对 ⇒ 依赖这些唯一键/非空的反证可能变化。
4. 未运行编译测试、未连库 ⇒ 全部为**静态代码级**结论；AUD-1 的"双记"也是推导而非运行时复现
   （批次32 用"修复后故障注入"给出正向证据，见该批记录 §六.2 的如实申报）。

## 四、执行原则

1. **逐条独立成批**：资金类改动不与其他条目混在一个 commit（便于回滚与定位）。
2. **先复核再修**：审计是外部输入，不是权威；每条动手前先确认"是否已被别处代码/约束/测试处理"。
3. **修一条补一条回归网**：缺陷修复必须留下能复现该缺陷形态的断言（含必要的注入点）。
4. **不成立也要落账**：把"排除过的怀疑"写下来，避免后人重复劳动。
